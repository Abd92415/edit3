import 'package:flutter/material.dart';

class SweetCategory extends StatelessWidget {
  const SweetCategory({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      backgroundColor: Colors.red,
    );
  }
}
